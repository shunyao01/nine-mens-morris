package model;

/**
 * TokenColor is an enumeration of the color of the token.
 */
public enum TokenColor {
    /**
     * a constant that indicates the token color is white for a particular player
     */
    WHITE,
    /**
     * a constant that indicates the token color is black for a particular player
     */
    BLACK
}
